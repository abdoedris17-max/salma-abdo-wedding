# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# Claude Code / local preview config
.claude/

# Original art sources & project brief — kept locally, not part of the
# published site (and not exposed in a public repo). Re-add if you want them
# backed up in a private repo.
Without Text/
Sample With Text/
CLAUDE-CODE-PROMPT.md

# Source layer exports (kept locally, not deployed)
assets_2x_png_version_only_blues_and_greens_pages_1/

# Heavy source illustration (kept locally, never committed)
assets/Artboard 11@2f2x.PNG

# New "no assets" artboard 16 sources + the cutout library (kept local, never
# committed — ~22 MB; only the compressed assets/invitation.* + assets/deco/*.webp ship)
Artboard No assets/
assets/Artboard 16@2.f5 no assets.png
assets/Artboard 16@3.f no assets.png
Artboard 11@2.f5.png

# Local analysis scratch + paste-helpers (regenerated by tools/, not shipped)
_work/
tools/_*.py
tools/deco_tags.html
